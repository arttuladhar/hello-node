pipeline {
    agent none
    environment {
        APPLICATION_NAME = 'hello-jenkins-pipeline'
    }

    stages {

        stage('build') {
            steps {
                echo "Hello World"
            }
        }
        
    }
}